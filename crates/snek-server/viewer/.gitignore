node_modules/

# Built bundle. `dist/index.html` is kept as a tracked placeholder so the server
# (which embeds dist/ via rust-embed) compiles without a frontend build; the real
# index.html + hashed assets are regenerated by `npm run build`.
dist/assets/
dist/*.tmp
