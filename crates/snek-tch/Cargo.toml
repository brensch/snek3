[workspace]

# Standalone (links libtorch via tch). Reuses the venv's PyTorch libtorch:
#   LIBTORCH_USE_PYTORCH=1 LIBTORCH_BYPASS_VERSION_CHECK=1
[package]
name = "snek-tch"
version = "0.1.0"
edition = "2021"

[dependencies]
tch = "0.24"
rand = "0.8"

[profile.release]
opt-level = 3
